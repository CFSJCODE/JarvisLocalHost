# J.A.R.V.I.S. sovereign runtime (Python 3.10)
# Learned semantic state is created locally from authorized PDFs only.

# Local API and upload handling
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
websockets==12.0
aiofiles==23.2.1
pydantic==2.7.1

# Host monitoring and deterministic PDF parsing
psutil==5.9.8
pymupdf==1.24.3
pdfplumber==0.11.0
Pillow==10.3.0

# From-scratch language model, retriever and ICM/PPO
# 2.4.1 is the exact torch release required by the optional DirectML stack.
torch==2.4.1
numpy==1.26.4

# Local utilities (none ships semantic/model weights)
tqdm==4.66.4
python-dotenv==1.0.1
rich==13.7.1
loguru==0.7.2
